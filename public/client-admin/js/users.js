(async () => { const formPanel = document.getElementById('userFormPanel'); const rows = document.getElementById('userRows'); await ClientAdmin.loadMe(); const load = async () => { const response = await ClientAdmin.api('/api/users'); if (!response || !response.ok) { rows.innerHTML = '<tr><td colspan="6">Unable to load users.</td></tr>'; return; } const users = await response.json(); rows.innerHTML = users.length ? users.map(user => `<tr><td>${user.username}</td><td>${user.email}</td><td>${user.roles?.join(', ') || '—'}</td><td>${user.enabled ? 'Enabled' : 'Disabled'}</td><td>${new Date(user.createdAt).toLocaleDateString()}</td><td><button class="refresh" data-edit="${user.id}">Edit</button></td></tr>`).join('') : '<tr><td colspan="6">No users found.</td></tr>'; rows.querySelectorAll('[data-edit]').forEach(button => button.addEventListener('click', () => openEdit(users.find(user => user.id === button.dataset.edit)))); }; function openEdit(user) { formPanel.hidden = false; formPanel.querySelector('h3').textContent = `Edit ${user.username}`; formPanel.querySelector('form').innerHTML = `<label>Username<input value="${user.username}" disabled></label><label>Email<input name="email" type="email" required value="${user.email}"></label><label>Status<select name="enabled"><option value="true" ${user.enabled ? 'selected' : ''}>Enabled</option><option value="false" ${!user.enabled ? 'selected' : ''}>Disabled</option></select></label><div class="actions"><button type="button" class="secondary" id="cancelEdit">Cancel</button><button type="submit" class="primary">Save changes</button></div>`; formPanel.querySelector('#cancelEdit').onclick = () => { formPanel.hidden = true; }; formPanel.querySelector('form').onsubmit = async event => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.target)); const response = await ClientAdmin.api(`/api/users/${user.id}`, { method: 'PUT', body: JSON.stringify({ email: values.email, enabled: values.enabled === 'true' }) }); if (response && response.ok) { formPanel.hidden = true; load(); } }; } document.getElementById('showUserForm').onclick = () => { formPanel.hidden = false; formPanel.querySelector('h3').textContent = 'Create user'; formPanel.querySelector('form').innerHTML = '<label>Username<input name="username" required minlength="3"></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required minlength="8"></label><div class="actions"><button type="button" class="secondary" id="cancelCreate">Cancel</button><button type="submit" class="primary">Create user</button></div>'; formPanel.querySelector('#cancelCreate').onclick = () => { formPanel.hidden = true; }; formPanel.querySelector('form').onsubmit = async event => { event.preventDefault(); const response = await ClientAdmin.api('/api/users', { method: 'POST', body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); if (response && response.ok) { formPanel.hidden = true; load(); } }; }; document.getElementById('refreshUsers').onclick = load; await load(); })();
